"""Output height of pyramid from input of blocks where the pyramid starts with one block at the top then +1
block each layer until as many blocks is used as possible"""

blocks = int(input("Enter the number of blocks: "))

height = 0

layers = 1

while layers <= blocks:
    height += 1
    blocks -= layers
    layers += 1

print("The height of the pyramid:", height)
